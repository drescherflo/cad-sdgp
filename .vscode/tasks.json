{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "setup_python_env",
            "type": "shell",
            "linux": {
                "command": "export CARB_APP_PATH=${config:isaacSimInstallFolder}/kit && export ISAAC_PATH=${config:isaacSimInstallFolder} && export EXP_PATH=${config:isaacSimInstallFolder}/apps && source ${config:isaacSimInstallFolder}/setup_python_env.sh && printenv >${config:isaacSimInstallFolder}/.vscode/.standalone_examples.env"
            }
        }
    ]
}
